# Spring Auto REST docs test

Launch with `./gradlew build`

`classic_rest_docs` should fail because `RestDocs` class does not have Javadoc, while still being documented using classic Spring REST docs.
